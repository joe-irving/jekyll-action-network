# frozen_string_literal: true

RSpec.describe Jekyll::ActionNetwork do
  it "has a version number" do
    expect(Jekyll::ActionNetwork::VERSION).not_to be nil
  end

  # Imports collection when import is string in config

  # Imports collection when import is hash

  # Gets API if key: in config

  # Gets API key if key not in config

  # Creates actions collection with all actions

  # maps feilds correctly

  # it "does something useful" do
  #   expect(false).to eq(true)
  # end
end
